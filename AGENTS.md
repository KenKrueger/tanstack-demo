# AGENTS.md

Notes for AI agents (and humans) running automated tasks in this repository.

## Running the App

```bash
pnpm dev        # starts Vite dev server on http://localhost:3001
pnpm build      # production build
pnpm typecheck  # TypeScript type-check (no emit)
```
